DEBUG=academy ./bin/www

set DEBUG=my-application followed by node ./bin/www  for Windows Machine

Manually need to create npm folder under AppData/Roaming for Windows. Seems it's bug in recent node installer. 